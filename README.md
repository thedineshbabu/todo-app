# todo-app

Sample React todo application used to exercise and test an agent-board workflow. It is intentionally minimal and includes obvious improvement opportunities (types, validation, styling, list keys, and related fixes) for automated agents to address.
